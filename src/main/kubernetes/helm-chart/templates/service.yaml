apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-cloud-k8s-issue.fullname" . }}
  labels:
    {{- include "spring-cloud-k8s-issue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-cloud-k8s-issue.selectorLabels" . | nindent 4 }}
